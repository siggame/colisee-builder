{
  "name": "colisee-template",
  "version": "0.0.1",
  "description": "template package for colisee sub-modules",
  "main": "src/index.js",
  "scripts": {
    "quick-start": "node src/index.js",
    "prestart": "npm run build",
    "start": "npm run quick-start",
    "build": "tsc",
    "setup": "npm install",
    "predocs": "npm run build",
    "docs": "npm run quick-docs",
    "quick-docs": "npm run quick-docs:ts && npm run quick-docs:api && npm run quick-docs:readme",
    "quick-docs:api": "apidoc --input src/ --output docs/api/",
    "quick-docs:ts": "typedoc --out docs/ts/",
    "quick-docs:readme": "cp README.md docs/",
    "quick-test": "mocha --colors test/",
    "pretest": "npm run build",
    "test": "npm run quick-test",
    "clean": "npm run clean:src && npm run clean:test && npm run clean:node_modules",
    "clean:src": "rm -f src/*.js && rm -f src/*.js.map && rm -f src/**/*.js && rm -f src/**/*.js.map",
    "clean:test": "rm -f test/*.js && rm -f test/*.js.map && rm -f test/**/*.js && rm -f test/**/*.js.map",
    "clean:node_modules": "rm -r -f node_modules/" 
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/siggame/colisee-template.git"
  },
  "author": "siggame",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/siggame/colisee-template/issues"
  },
  "homepage": "https://github.com/siggame/colisee-template#readme",
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/dotenv": "^2.0.20",
    "@types/express": "^4.0.35",
    "@types/lodash": "^4.14.52",
    "@types/mocha": "^2.2.39",
    "@types/node": "^7.0.5",
    "apidoc": "^0.17.5",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "typedoc": "^0.5.7"
  },
  "dependencies": {
    "apidoc": "^0.17.5",
    "dotenv": "^4.0.0",
    "express": "^4.14.1",
    "lodash": "^4.17.4",
    "pg": "^6.1.2",
    "typescript": "^2.1.5"
  }
}
